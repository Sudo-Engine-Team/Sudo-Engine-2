package site.root3287.sudo2.UI.events.listeners;

import site.root3287.sudo2.UI.events.UIClickEvent;
import site.root3287.sudo2.events.Listener;

public interface UIClickListener extends Listener {
	public boolean onClick(UIClickEvent e);
}
